export class Utility {

    sortArray<T>(arg: Array<T>): Array<T> {
        console.log(arg.length);
        return arg;
    }
    filterArray<T>(arg: Array<T>): Array<T> {
        console.log(arg.length);
        return arg;
    }
}