export interface IIndustry {
    "code": number;
    "name": string;
}
export interface ITiming {
    url: string;
    companyId: string;
    networkLatency: string;
    redirectTime: string;
    pageLoadTime: string;
    completeTime: string;
}