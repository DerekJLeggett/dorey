export interface IIndustry {
    "id": number;
    "name": string;
}
export interface ITiming {
        url: string;
        companyId: string;
        networkLatency: string;
        redirectTime: string;
        pageLoadTime: string;
        completeTime: string;
}